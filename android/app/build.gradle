plugins {
    id "com.android.application"
    id "kotlin-android"
    // The Flutter Gradle Plugin must be applied after the Android and Kotlin Gradle plugins.
    id "dev.flutter.flutter-gradle-plugin"
}

android {
    namespace = "com.example.news_ownapi"
    compileSdk = flutter.compileSdkVersion
    ndkVersion = flutter.ndkVersion

    compileOptions {
        sourceCompatibility = JavaVersion.VERSION_1_8
        targetCompatibility = JavaVersion.VERSION_1_8
    }

    kotlinOptions {
        jvmTarget = JavaVersion.VERSION_1_8
    }

    defaultConfig {
        applicationId = "com.example.news_ownapi"
        minSdk = 23
        targetSdk = flutter.targetSdkVersion
        versionCode = flutter.versionCode
        versionName = flutter.versionName
    }
    signingConfigs {
        release {
            storeFile file('C:/Users/Joel-a/Desktop/ACLib/ownapi/news_ownapi/ownapi.jks')
            storePassword "maynenamikaze12"
            keyAlias "ownapi"
            keyPassword "maynenamikaze12"
        }
    }

   buildTypes {
        release {
            signingConfig signingConfigs.release
            minifyEnabled true
            shrinkResources true
            proguardFiles getDefaultProguardFile('proguard-android-optimize.txt'), 'proguard-rules.pro'
        }
    }
}

flutter {
    source = "../.."
}

// Apply the Firebase plugin here
apply plugin: 'com.google.gms.google-services'

dependencies {
    // Add Firebase dependencies here
    implementation 'com.google.firebase:firebase-analytics:21.3.0'  // Example, add more as needed
    implementation 'com.google.firebase:firebase-auth:21.0.5'  // Example, add more as needed
}
